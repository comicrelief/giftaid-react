import { NumericBool } from '../../../validation';
/**
 * Props required to generate
 * an Transaction model.
 */
export interface TransactionProps {
    /** Transaction UUID */
    uuid?: string;
    /** Order reference. Required by `serverless-payments` */
    order_reference: string;
    /** Prize Contentful ID */
    prize_id: string;
    /** Whether this is a postal entry */
    postal?: NumericBool;
    /** Postal Ticket ID if any */
    postal_id?: string | null;
    /** Associated supporter first name */
    firstname: string;
    /** Associated supporter last name */
    lastname: string;
    /** Associated supporter email */
    email?: string;
    /** Associated supporter mobile */
    mobile?: string;
    /** Associated supporter identity hash */
    identity_hash: string;
    /** Transaction creation timestamp */
    timestamp?: string;
}
/**
 * Transaction model.
 *
 * Identifies a ticket group transaction.
 * Multiple tickets entries may share a transaction.
 */
export declare const Transaction: import("../../../validation").Model<TransactionProps>;
/**
 * Generates the S3 Key for the parquet file.
 *
 * @param prize_id
 * @param transaction_uuid
 * @param unixTimestamp
 */
export declare const s3Key: (prize_id: string, transaction_uuid: string, unixTimestamp: number) => string;
//# sourceMappingURL=Transaction.d.ts.map