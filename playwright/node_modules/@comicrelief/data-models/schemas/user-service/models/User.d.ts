export interface UserBasicProps {
    uuid?: string;
    firstName?: string;
    lastName?: string;
}
/**
 * Props required to generate
 * an User model.
 */
export interface UserProps extends UserBasicProps {
    active?: boolean;
    created?: Date;
    updated?: Date;
}
/**
 * User model.
 *
 * Identifies a user. Exposes a `uuid` field
 * that can be used as AWS::Cognito username.
 * Stores `firstName` and `lastName`.
 *
 * Includes metadata regarding the identity:
 * - active: Whether the user is marked as active
 * - created: Timestamp of creation
 * - updated: Timestamp of last update
 */
export declare const User: import("../../../validation").Model<UserProps>;
//# sourceMappingURL=User.d.ts.map