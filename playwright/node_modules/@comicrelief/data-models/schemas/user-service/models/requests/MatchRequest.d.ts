import type { UserProps } from '../User';
export declare enum MatchTypes {
    username = "username",
    email = "email",
    mobile = "mobile",
    identity_hash = "identity_hash"
}
export declare const MatchTypesList: MatchTypes[];
/**
 * Props required to generate
 * an MatchRequest.
 */
export interface MatchRequestProps {
    username?: string;
    email?: string;
    mobile?: string;
    identity_hash?: string;
}
/**
 * MatchRequest model.
 *
 * Specifies the payload to a request
 * to UserService's match lambda.
 *
 * One and only one of MatchTypes
 * must be provided, and be valid.
 */
export declare const MatchRequest: import("../../../../validation").Model<MatchRequestProps>;
/**
 * Represents the response body
 * for a MatchResponse
 */
export interface MatchResponse {
    hash: string;
    type: MatchTypes;
    user: UserProps;
}
//# sourceMappingURL=MatchRequest.d.ts.map