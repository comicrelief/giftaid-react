import { NumericBool } from '../../../../../validation';
import { ErpResources } from '../../common';
import { ErpProductVariantProps } from './ProductVariant';
/**
 * Name of this doctype in ERP.
 */
export declare const DOCTYPE = ErpResources.PRODUCT;
/**
 * Type representing the `Product` doctype in ERP.
 */
export interface ErpProductProps {
    /** Unique product ID within Shopify. */
    shopify_id: string;
    /** The name of the product. */
    title: string;
    /** List of variants. */
    variants: ErpProductVariantProps[];
    /** Whether this product is available. */
    available: NumericBool;
    /** The product's first option for variants, if applicable. */
    option1: string | null;
    /** The product's second option for variants, if applicable. */
    option2: string | null;
    /** The product's third option for variants, if applicable. */
    option3: string | null;
}
/**
 * Model for a `Product` document in ERP.
 */
export declare const ErpProduct: import("../../../../../validation").Model<ErpProductProps>;
export declare const DOCTYPE_METADATA: {
    doctype: {
        name: ErpResources;
        autoname: string;
        title_field: string;
    };
    fields: {
        variants: {
            options: string;
        };
        available: {
            default: string;
        };
    };
};
//# sourceMappingURL=Product.d.ts.map