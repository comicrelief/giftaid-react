import { ErpCurrency } from '../../../../../validation';
import { ErpResources } from '../../common';
/**
 * Name of this doctype in ERP.
 */
export declare const DOCTYPE = ErpResources.SHOPIFY_ORDER_LINE_ITEM;
/**
 * Type representing the `Shopify Order Line Item` doctype in ERP.
 */
export interface ErpShopifyOrderLineItemProps {
    /** Shopify ID of the item ordered. Links to `Product Variant`. */
    variant_id: string | null;
    /** Stock unit identifier of the item. */
    sku: string | null;
    /** Description of the item for human convenience. */
    title: string;
    /** How many were ordered. */
    quantity: number;
    /** Price at time of purchase, including VAT if applicable. */
    price: ErpCurrency;
    /** Taxes (VAT) on this item. */
    vat: ErpCurrency;
    /** Sum of any discounts applied to this item. */
    discount: ErpCurrency;
    /** Currency for `price`, `vat`, and `discount`. Should always be 'GBP'. */
    currency: string;
}
/**
 * Model for a `Shopify Order Line Item` document in ERP.
 */
export declare const ErpShopifyOrderLineItem: import("../../../../../validation").Model<ErpShopifyOrderLineItemProps>;
//# sourceMappingURL=ShopifyOrderLineItem.d.ts.map