import { ErpResources } from '../../common';
/**
 * Name of this doctype in ERP.
 */
export declare const DOCTYPE = ErpResources.PRIZE;
/**
 * Frappe client script to be associated
 * with the Prize model
 */
export declare const PrizeClientScript = "\nfrappe.ui.form.on('Prize', {\n  refresh: function(frm) {\n      // use the __islocal value of doc,  to check if the doc is saved or not\n      var isEditable = frm.doc.__islocal || frm.doc.triggered_tickets_pick === 0;\n      frm.set_df_property('triggered_tickets_pick', 'read_only', isEditable ? 0 : 1)\n  }\n});\n";
export * from '../../../../prize-platform/models/Prize';
//# sourceMappingURL=Prize.d.ts.map