import aws from 'aws-sdk';
import { Tester } from '../../Tester';
/**
 * Identifies an S3 reference
 */
interface S3Reference {
    Bucket: string;
    Key: string;
}
/**
 * Manages cleanup of S3 files
 * during feature tests.
 *
 * @class
 */
export declare class S3Tester extends Tester<S3Reference> {
    client: aws.S3;
    constructor();
    /**
     * Performs a HEAD request.
     *
     * @param bucket
     * @param key
     */
    head(bucket: string, key: string): Promise<aws.S3.HeadObjectOutput>;
    /**
     * Lists all objects in the given prefix
     *
     * @param bucket
     * @param prefix
     */
    list(bucket: string, prefix?: string): Promise<aws.S3.ObjectList | undefined>;
    /**
     * @inheritdoc
     */
    removeReference(reference: S3Reference): Promise<void>;
    /**
     * Deletes all objects found
     * in the specific prefix.
     * Useful to clean up the Athena bucket
     * of the query results.
     *
     * @param bucket
     * @param prefix
     */
    clearPath(bucket: string, prefix: string): Promise<void>;
}
export {};
//# sourceMappingURL=S3Tester.d.ts.map